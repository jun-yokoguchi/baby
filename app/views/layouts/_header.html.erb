<header class="navbar">
  <div class="container">
	  <nav>
	    <div class="container-fluid">
	      <div class="navbar-header">
          <div class="logo">
            <%= link_to "Jammies", root_path, id: "logo" %>
	 　     </div>
	 　   </div>
	      <div class="collapse navbar-collapse">
          <ul class ="nav navbar-nav navbar-right">
            <li><%= link_to "ホーム",  root_path %></li>
      　      <%# <p class="navbar-text navbar-right">ようこそ <a href="#" class="navbar-link"</a> さん。</p>%>
           <li><%= link_to "ガイド",  '#' %></li>
            <% unless user_signed_in? %>
              <li><%= link_to "会員登録", new_user_registration_path %></li>
            <% end %>
            <% if user_signed_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                アカウント <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "会員ページ", current_user %></li>
                  <li><%= link_to "設定", edit_user_registration_path %></li>
                  <li class="divider"></li>
                  <li><%= link_to "ログアウト", destroy_user_session_path, method: "delete" %></li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "ログイン", new_user_session_path %></li>
            <% end %>
          </ul>
          <li><button class="btn btn-default" type="button"><a href="#"><span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"><span class="badge">1</span></span></a></button></li>
            <%#カートのカウントを読み込む%>
        </div>
      </div>
    </nav>
  </div>
</header>